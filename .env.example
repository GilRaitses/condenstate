# condenstate: env for local agent with PHY600 + AWS access
# Copy to .env and set values. Do not commit .env (secrets/location-specific).
# Used so the same laptop agent can work in both PHY600 and condenstate.
# For OpenAI Codex (cloud): set these in Codex's project/agent env; see .meta/docs/ENV_FOR_CODEX.md.

# Magniphyq dashboard IP (optional; if unset, resolve via AWS CLI)
# Get current: aws ec2 describe-instances --region us-east-1 --filters "Name=tag:Name,Values=magniphyq" --query 'Reservations[*].Instances[*].PublicIpAddress' --output text
# MAGNIPHYQ_IP=3.81.174.42

# Path to PHY600 repo on this machine (for pipeline, registry, data)
# PHY600_ROOT=~/PHY600

# SSH key for EC2 (same as PHY600 pipeline)
# SSH_KEY_PATH=~/.ssh/pax-ec2-key.pem
